package com.rapit.client.module.modules.misc;

import com.rapit.client.module.Category;
import com.rapit.client.module.Module;

/** PackDisplay – shows the active resource pack name on the HUD. */
public class PackDisplay extends Module {
    public PackDisplay() { super("PackDisplay","Show active resource pack on HUD.",Category.MISC); }
}
